{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Terminal de depuración de JavaScript",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}